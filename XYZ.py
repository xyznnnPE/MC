import math
import time
import position
class XYZT():
        def __init__X(position.n):
           this.n = super.position
        def __init__Y(position.n):
           this.n = super.position
        def __init__Z(position.n):
           this.n = super.position
        def __init__T(position.n):
           this.n = super.position
def space(x,y,z,X,Y,Z):
        
        this.x = new
        if (X.position + 1),(Y.posssition),(Z.position)
        return key.D
        else (X.position - 1),(Y.posssition),(Z.position)
        return key.A
        
        this.y = new 
        if (X.position),(Y.position + 1),(Z.position)
        return key.W
        else (X.position),(Y.position - 1),(Z.position)
        return key.S
        
        this.z = new 
        if (X.position),(Y.position),(Z.position + 1)
        return key.space
        else (X.position),(Y.position),(Z.position - 1)
        return key.shift
