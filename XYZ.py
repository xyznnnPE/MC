import math
import time
import position
class XYZT():
        def __init__X(n):
           thisn = super.position
        def __init__Y(n):
           thisn = super.position
        def __init__Z(n):
           thisn = super.position
        def __init__T(n):
           thisn = super.position
class space(x,y,z,X,Y,Z,key):
      def X(this):  
        this.x = (X.position + 1),(Y.posssition),(Z.position)
        return key.D
      def X(that):
        that.x = (X.position - 1),(Y.posssition),(Z.position)
        return key.A
      def Y(this):
        this.y = (X.position),(Y.position + 1),(Z.position)
        return key.W
      def Y(that):
        that.y = (X.position),(Y.position - 1),(Z.position)
        return key.S
      def Z(this):
        this.z = (X.position),(Y.position),(Z.position + 1)
        return key.space
      def Z(that):
        that.z = (X.position),(Y.position),(Z.position - 1)
        return key.shift
